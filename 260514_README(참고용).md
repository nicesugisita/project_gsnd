# 백엔드 코드 설명

> 경상남도청 RAG 챗봇 백엔드 — FastAPI 기반 AI 채팅 API 서버

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [핵심 용어 정리](#2-핵심-용어-정리)
3. [기술 스택](#3-기술-스택)
4. [디렉토리 구조](#4-디렉토리-구조)
5. [앱 기동 순서 (Entry Point)](#5-앱-기동-순서-entry-point)
6. [설정 관리](#6-설정-관리)
7. [도메인별 구조](#7-도메인별-구조)
8. [핵심: 채팅 파이프라인 흐름](#8-핵심-채팅-파이프라인-흐름)
9. [RAG 파이프라인 상세 (general)](#9-rag-파이프라인-상세-general)
10. [Mariner 검색 엔진](#10-mariner-검색-엔진)
11. [부가 기능](#11-부가-기능)

---

## 1. 프로젝트 개요

경상남도청 홈페이지 AI 챗봇 **백엔드 서버**. 사용자 복지·행정 질문에 대해 문서 검색(RAG) 후 AI 답변을 생성·스트리밍합니다.

**주요 기능**

- 경상남도 복지·행정 정보 질의응답
- 시·군별 특화 복지 서비스 안내
- 생애주기(나이대) 기반 맞춤 복지 추천
- SSE 스트리밍 응답
- STT / TTS 지원
- 업로드 문서 기반 QA
- "더 알려줘" 후속 질문(MORE_INFO / MORE_DETAIL) 처리
- 정책 우선순위 키워드 기반 결과 부스팅 (`policy_priority`)

---

## 2. 핵심 용어 정리

### RAG (Retrieval-Augmented Generation)
실제 문서를 먼저 검색한 뒤 그 내용을 참고해 AI가 답변하는 방식. 이 시스템의 핵심 기술.

### 의도 (Intent)
LLM이 사용자 질문을 분류한 결과. 의도에 따라 검색 파이프라인과 프롬프트가 달라짐.

| 의도 값 | 설명 | 예시 질문 |
|---|---|---|
| `general` | 일반 복지·행정 정보 질문 | "기초생활수급자 신청 방법은?" |
| `comparison` | 두 서비스 간 비교 | "장애인연금과 장애수당의 차이는?" |
| `guide_recommend` | 사용자 맞춤 복지 추천 | "저에게 맞는 복지 혜택 추천해 주세요" |
| `search` | 특정 키워드 단순 검색 | "창원시 의창구 행정복지센터 연락처 알려줘" |

### 후속 의도 (Next Intent)
직전 응답에 대한 후속 발화 분류 — `classify_next_intent()` 결과. 의도 재분류 없이 직전 검색을 재사용·확장할 수 있게 함.

| 후속 의도 | 의미 |
|---|---|
| `MORE_INFO` | "더 알려줘" — 동일 주제로 신규 문서 추가 검색 (직전 노출 문서·서비스명 제외) |
| `MORE_DETAIL` | "○○○에 대해 더 자세히" — 특정 서비스 상세 |
| `NEW_SEARCH` | 새로운 주제로 전환 — 직전 검색 결과 재사용 안 함 |
| `REFINE_SEARCH` | 같은 주제 내 조건 변경 |
| `CLARIFY_REPLY` | 챗봇 되묻기에 대한 답변 — `query_recreation`에서 원질문과 합성 |

### 검색 컬렉션

| 컬렉션 | 내용 |
|---|---|
| **OKMS** | 경상남도 복지 서비스 카드 |
| **GOV_OKMS** | 국가·정부 기관 복지 서비스 카드 |
| **GSND** (DATASET_V8) | 경남도청 일반 행정 문서 (공지사항, 보도자료 등) |
| **WELFARE_TEL** | 읍·면·동 단위 복지 기관 연락처 |
| **WELFARE** | 복지 시설 정보 |
| **UPLOAD** | 사용자 업로드 문서 |

### 시·군 (Si-Gun)
경상남도 내 행정구역 단위(창원시, 진주시, 고성군 등). 복지 서비스가 지역마다 다르므로 검색 필터로 사용. `app/mariner/sigun_utils.py`에서 정규화.

### 생애주기 (Lifecycle)
출생연도 기반 나이대(영아기·아동기·청소년기·청년기·중장년기·노년기). 맞춤 복지 추천 필터로 사용. **만 18세는 청년기로 분류** (`_birth_year_to_lifecycle`).

### 정책 우선순위 (Policy Priority)
실시간 변경 가능한 키워드 매핑 테이블(`policy_priority`). 특정 태그(`implant`, `low_income`, `elderly_benefits`)에 해당하면 매칭 문서를 가중치 부스팅. 

### Mariner
Java 기반 사내 벡터 검색 엔진. JPype를 통해 Python에서 JVM을 직접 실행.

### SSE (Server-Sent Events)
생성되는 텍스트를 실시간으로 클라이언트에 스트리밍하는 방식.

---

## 3. 기술 스택

| 영역 | 기술 | 비고 |
|---|---|---|
| 웹 프레임워크 | FastAPI (Python 3.12) | 비동기 HTTP API 서버 |
| 설정 관리 | pydantic-settings (.env 기반) | 환경변수로 설정값 주입 |
| LLM 연동 | httpx AsyncClient | 내부 OpenAI 호환 API 비동기 호출 |
| 벡터 검색 엔진 | Mariner (Java 기반) | JPype로 Python에서 JVM 직접 호출 |
| DB | MySQL | mysql-connector-python, 커넥션 풀 |
| 비동기 HTTP | httpx | LLM, DeepServer, TTS 클라이언트 공유 풀 |
| STT | WebSocket 기반 외부 서버 | 음성 → 텍스트 변환 |
| TTS | HTTP 기반 외부 서버 | 텍스트 → 음성 변환 |
| 형태소 분석 | kiwi | 키워드(명사) 추출에 사용 |
| 토큰 계산 | tiktoken | LLM 입력 토큰 제한 처리 |

---

## 4. 디렉토리 구조

```
src/app/
├── __init__.py              # register_routes() — 모든 도메인 라우터 등록
├── main.py                  # FastAPI 앱 인스턴스 (uvicorn 진입점)
├── application.py           # create_app() 팩토리 (대체 진입점)
├── dependencies.py          # 공통 의존성 주입, 히스토리 저장 유틸
│
├── core/                    # 앱 전역 설정·인프라
│   ├── config.py            # Settings (pydantic-settings, 싱글톤)
│   ├── lifespan.py          # startup/shutdown 자원 초기화
│   ├── constants.py         # 전역 상수 (ROLE_USER 등, 로그 상수)
│   ├── exceptions.py        # 커스텀 예외
│   ├── logging_context.py   # 요청별 로그 컨텍스트 (conv_id, user_id)
│   └── protocols.py         # Protocol 타입 정의 (인터페이스)
│
├── shared/                  # 도메인 간 공유 코드
│   ├── schemas.py           # 공통 Pydantic 모델 (ChatRequest 등)
│   ├── db/connection.py     # MySQL 단건 연결 팩토리
│   └── utils/
│       ├── prompt_loader.py     # 프롬프트 파일 로더 (lru_cache)
│       ├── keyword_extractor.py # kiwi 명사 추출
│       ├── year_filter.py       # 연도 필터 추출
│       ├── relevance_filter.py  # 관련성 필터 (LLM 무관 문서 제거)
│       ├── status_messages.py   # SSE status 메시지 헬퍼
│       ├── streaming.py         # SSE 스트리밍 유틸
│       └── helpers.py           # 응답 빌더, error_detail 등
│
├── chat/                    # 핵심 도메인 — 채팅 응답 생성
│   ├── router.py            # POST /v1/chat/completions, /recommended-question, /recommend/collect, /suggest-questions
│   ├── service.py           # 서비스 퍼사드 (re-export 모음)
│   ├── schemas.py           # chat 도메인 전용 스키마
│   │
│   ├── _pipeline_steps.py   # 파이프라인 공통 단계 함수 (PreCheck, Sigun, Preprocess 등)
│   ├── _streaming.py        # 스트리밍 흐름 전체 구현
│   ├── _helpers.py          # RAG 모드 분기, 비RAG 처리, query_recreation 래퍼
│   ├── _stream_utils.py     # SSE 인코딩 헬퍼
│   ├── _conversation_ctx.py # 대화 세션 초기화 + 동시 사용자 제한
│   ├── _doc_filter.py       # 응답 후 referenced_documents 후처리
│   │
│   ├── preprocessing.py     # 통합 전처리 (단일 LLM 호출 → 정제·의도·재구성·확장)
│   ├── routing.py           # 쿼리 재구성·확장·트리플 추출·next_intent 분류
│   ├── query_reform.py      # DeepServer 기반 query rewriting (PROMPT_ID=21)
│   ├── sigun.py             # 시·군 추출, 되묻기 처리
│   ├── lifecycle.py         # 생애주기 추출 (출생연도 기반, 히스토리 추적 포함)
│   ├── intent_registry.py   # 의도 메타 단일 진실 공급원 (라벨·재사용 규칙)
│   ├── more_results.py      # 직전 preprocess/exclusion 히스토리 추출
│   ├── mariner_index.py     # 업로드 문서 Mariner 색인 트리거 (trigger_mariner_index)
│   ├── recommended_question_utils.py # 추천 후속 질문 — referenced_service_names/chunk_ids 합성
│   ├── suggest_questions_service.py  # /suggest-questions 서비스 구현
│   ├── user_session.py      # 동시 사용자 제한 (MAX_CONCURRENT_USERS) 세션 추적
│   │
│   └── infra/
│       ├── retriever.py     # MarinerRetriever (검색 엔진 인스턴스 wrapper)
│       ├── llm/
│       │   ├── client.py        # LLM HTTP 클라이언트 (모듈 전역 _llm_client)
│       │   ├── core.py          # call_llm_api() 핵심
│       │   ├── judgment.py      # pre_check, ask_judgment, rag_norag 등 판단 함수
│       │   └── preprocessing.py # convert_korean_to_standard 등 정제 함수
│       ├── deepserver/
│       │   └── client.py    # DeepServer HTTP 클라이언트 (expand_query, triples 등)
│       ├── stt/
│       │   └── client.py    # STT WebSocket 클라이언트
│       ├── db/
│       │   ├── welfare.py   # 복지 시설 DB 조회
│       │   └── welfare_tel.py # 복지 연락처 DB 조회
│       └── rag/             # ★ RAG 파이프라인
│           ├── __init__.py              # 패키지 re-export
│           ├── pipeline_general.py      # general 의도 파이프라인
│           ├── pipeline_comparison.py   # comparison 의도 파이프라인
│           ├── pipeline_guide_recommend.py # guide_recommend 파이프라인
│           ├── pipeline_search.py       # search 의도 파이프라인
│           ├── pipeline_recommended_question.py # 추천 후속 전용 파이프라인
│           ├── pipeline_utils.py        # 파이프라인 공통 헬퍼 (병렬 수집기 등)
│           ├── common.py                # 정렬·후처리·exclusion 필터
│           ├── document.py              # 문서 포맷팅 (프롬프트용)
│           ├── extraction.py            # sigun/lifecycle/year 추출
│           ├── query_builder.py         # 키워드 검색 문자열 구성, OKMS 키워드 필터
│           ├── collection.py            # 컬렉션 스키마 식별
│           ├── token.py                 # tiktoken 기반 토큰 자르기
│           ├── expansion_cap.py         # 확장 쿼리 중복 제거·캡
│           ├── policy_config.py         # 정책 우선순위 설정 상수
│           ├── policy_priority.py       # 정책 우선순위 캐시·백그라운드 워커
│           └── response_generator.py    # 최종 응답 생성 (generate_final_response_v2)
│
├── conversation/            # 대화 이력 관리 도메인
│   ├── router.py            # GET/POST/DELETE /v1/chat/conversations/*
│   ├── service.py           # 대화 서비스 퍼사드
│   ├── history.py           # 히스토리 조회·저장
│   ├── crud.py              # 대화방 CRUD
│   ├── feedback.py          # like/dislike/copy 피드백
│   └── repository.py        # DB 액세스 계층
│
├── document/                # 문서 업로드·다운로드·요약 도메인
│   ├── router.py            # /v1/files/upload, /v1/documents/* 등록
│   ├── service.py           # 서비스 인터페이스
│   ├── service_impl.py      # 서비스 구현
│   ├── _upload.py           # 파일 업로드 처리
│   ├── _download.py         # 문서 다운로드, 텍스트 추출, 요약
│   ├── _ask.py              # 업로드 문서 기반 QA
│   ├── summary.py           # 요약 로직
│   ├── transfer.py          # 파일 전송 헬퍼
│   └── uploaded.py          # 업로드 메타 관리
│
├── mariner/                 # Mariner 벡터 검색 엔진 연동
│   ├── jvm_manager.py       # JPype JVM 싱글톤 초기화·관리
│   ├── parsing.py           # Mariner 응답 파싱
│   ├── sigun_utils.py       # 시군명 정규화 유틸
│   ├── queryset_okms.py     # OKMS — 경남 복지 서비스 카드 (벡터+키워드 듀얼, fallback 옵션 포함)
│   ├── queryset_gov_okms.py # GOV_OKMS — 국가·정부 기관 복지 서비스 카드
│   ├── queryset_gsnd.py     # GSND — 경남도청 일반 행정 문서 (DATASET_V8)
│   ├── queryset_welfare.py  # WELFARE — 복지 시설 정보
│   ├── queryset_welfare_tel.py # WELFARE_TEL — 읍·면·동 복지 연락처
│   ├── queryset_recommended_question.py # 추천 질문 전용 검색 쿼리셋
│   └── queryset_upload.py   # UPLOAD — 사용자 업로드 문서
│
├── tts/                     # Text-to-Speech 도메인
│   ├── router.py            # /api/tts/* 라우트 (STT WS 포함)
│   ├── service.py           # TTS HTTP 요청 (외부 서버)
│   └── infra/
│       ├── pipeline.py      # PreprocessingPipeline (정규화→분절→토큰)
│       ├── config_loader.py # 정규화 설정 로더
│       ├── wav_utils.py     # WAV 처리 유틸
│       ├── tts_helpers.py   # TTS 요청·base64 처리 헬퍼
│       ├── normalizer/      # 텍스트 정규화 (KO/EN, 숫자·날짜·주소·전화 등)
│       ├── parser/          # 텍스트 파서
│       └── segmenter/       # 문장 분절 (classifier·splitter·merger·token_manager)
│
├── rag/                     # RAG 보조 API (DeepServer 프록시)
│   ├── router.py            # /v1/query/* — extract-triples, expand, re-query, comparison-* (7-4 참조)
│   ├── service.py
│   └── schemas.py
│
├── keyword/                 # 키워드 추출 API
│   └── router.py            # POST /v1/keywords/extract
│
└── system/                  # 시스템 상태 API
    └── router.py            # GET /health
```

---

## 5. 앱 기동 순서 (Entry Point)

**설명 시작점**: `uvicorn app.main:app` → `src/app/main.py` (또는 `application.create_app()`)

```
uvicorn app.main:app
    └─ create_app()
        ├─ _setup_logging()       # PID별 RotatingFileHandler, RequestContextFilter
        ├─ FastAPI 앱 생성 + CORS 미들웨어
        ├─ 도메인 라우터 7개 등록 (chat, rag, conversation, document, tts, keyword, system)
        └─ lifespan 연결
```

**lifespan (startup)** — `src/app/core/lifespan.py`

1. `_init_jvm()` — Mariner JVM 기동 (JPype, JAR 파일 로드)
2. `_init_llm_client()` — LLM httpx 커넥션 풀 (`app.state.llm_client`, 모듈 전역에도 주입)
3. `_init_ds_client()` — DeepServer httpx 커넥션 풀 (`app.state.ds_client`)
4. `_init_db_pool()` — MySQL 커넥션 풀 (`app.state.db_pool`)
5. `_init_retriever()` — MarinerRetriever 인스턴스 (`app.state.retriever`)
6. `_preload_prompts()` — 프롬프트 파일 `lru_cache` 워밍업
7. `_start_policy_priority_refresh_worker()` — 정책 우선순위 키워드 캐시 + 주기 갱신 워커

**lifespan (shutdown)**

- LLM/DeepServer httpx 클라이언트 `aclose()`
- DB 풀 내부 커넥션 개별 해제
- 정책 우선순위 워커 종료

### `app.state` 자원 공유 패턴

httpx 커넥션 풀·DB 풀·MarinerRetriever처럼 매 요청마다 새로 만들면 비싼 자원은 startup 때 1회만 생성해 **FastAPI 앱 인스턴스의 속성**(`app.state`)에 매달아 두고 모든 핸들러가 공유합니다.

```python
# lifespan.py (startup)
app.state.llm_client = httpx.AsyncClient(...)   # 커넥션 풀
app.state.db_pool    = MySQLConnectionPool(...)
app.state.retriever  = MarinerRetriever()

# 핸들러
@router.post("/v1/chat/completions")
async def handler(request: Request):
    client = request.app.state.llm_client       # 같은 풀 재사용
    ...
```

> **주의**: `app.state`는 **디렉토리가 아닙니다**. Starlette `FastAPI` 객체의 속성이며, `src/app/state/` 같은 폴더는 존재하지 않습니다. 패키지명도 `app`(`src/app/`), FastAPI 인스턴스 변수명도 `app`(`main.py`의 `app = create_app()`)이라 같은 단어가 두 가지를 가리켜 헷갈리기 쉬우니 주의하세요. `app.state`에 무엇이 들어 있는지는 `lifespan.py` startup 코드만 보면 됩니다.

이 코드베이스는 호환을 위해 `app.state.llm_client` 저장과 동시에 `app/chat/infra/llm/client.py` 모듈 전역 `_llm_client`에도 같은 인스턴스를 주입합니다 (`lifespan.py:64`). 둘 중 어느 쪽으로 접근해도 동일 객체.

---

## 6. 설정 관리

**파일**: `src/app/core/config.py` (pydantic-settings, `.env` 기반). 우선순위: 환경변수 > `.env` > 필드 기본값. `Config = get_settings()` 싱글톤(`@lru_cache`)으로 노출되어 코드 어디서나 `Config.XXX` 형태로 접근.

| 그룹 | 키 | 설명 |
|---|---|---|
| 서버 | `HOST`, `PORT`, `DEBUG`, `LOG_LEVEL`, `ENVIRONMENT` | `ENVIRONMENT=development\|production\|testing` |
| 앱 | `APP_NAME`, `APP_VERSION`, `APP_DESCRIPTION`, `MODEL_NAME` | FastAPI 메타·기본 모델명 |
| LLM | `LLM_ENABLED`, `LLM_API_URL`, `LLM_API_TIMEOUT`, `RELEVANCE_LLM_API_URL`, `RELEVANCE_LLM_MODEL_NAME`, `FIXED_LLM_SEED`, `NEXT_INTENT_LLM_ENABLED` | 메인/관련성 판단 LLM, 후속 의도 분류 토글 |
| RAG | `RAG_ENABLED`, `RAG_COLLECTION` (GSND), `RAG_OKMS_COLLECTION`, `RAG_GOV_OKMS_COLLECTION`, `RAG_WELFARE_CENTER_COLLECTION`, `RAG_WELFARE_TEL_COLLECTION`, `RAG_THRESHOLD`, `RAG_NUM_REFERENCED_DOCS`, `RAG_UI_MAX_DOCS`, `RAG_USE_QA_WHEN_EMPTY`, `QUERY_REWRITING_ENABLED` | 컬렉션·임계값·노출 건수, query rewriting A/B 토글 |
| Mariner | `MARINER_IP`, `MARINER_PORT`, `MARINER_TIMEOUT`, `MARINER_THRESHOLD`, `MARINER_MAX_RESULTS`, `MARINER_LOCAL_HOST`, `MARINER_LOCAL_PORT`, `MARINER_INDEX_API_URL`, `MARINER_UPLOAD_COLLECTION`, `MARINER_INDEX_TIMEOUT` | 검색·인덱스 API 접속 |
| JVM/JAR | `JAR_LIB_PATH`, `JF_ATTACHER_CONF_PATH`, `JF_ATTACHER_NATIVE_LIB_PATH`, `JAVA_MIN_MEMORY`, `JAVA_MAX_MEMORY` | JPype JVM 옵션 |
| DB | `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `OKMS2_DB_NAME`, `DB_POOL_SIZE`, `DB_POOL_NAME`, `DB_CONNECTION_TIMEOUT` | MySQL/MariaDB 접속·풀 설정 |
| Policy Priority | `POLICY_PRIORITY_DB_ENABLED`, `POLICY_PRIORITY_TABLE`, `POLICY_PRIORITY_CACHE_TTL_SEC`, `POLICY_PRIORITY_CHANGE_CHECK_SEC`, `POLICY_RULES_PATH` | 정책 우선순위 캐시·YAML 규칙 |
| DeepServer | `DEEP_SERVER_URL`, `DEEPSERVER_TIMEOUT`, `MULTITURN_MAX_TURNS`, `MULTITURN_ASSISTANT_SUMMARY_LEN` | 외부 서버·멀티턴 입력 제한 |
| STT | `STT_WEBSOCKET_URL`, `STT_PING_INTERVAL`, `STT_PING_TIMEOUT`, `STT_CLOSE_TIMEOUT`, `STT_CONNECTION_TIMEOUT` | STT WS 접속·타임아웃 |
| TTS | `TTS_SERVER_URL`, `TTS_MASTER_KEY`, `TTS_REQUEST_TIMEOUT`, `SID`, `TEMPO`, `PAD_SILENCE`, `AMPLIFY`, `GAIN_DB` | TTS HTTP 접속·합성 옵션 |
| 전처리 토글 | `TEXT_CLEANING_ENABLED`, `KOREAN_STANDARDIZATION_ENABLED` | 입력 정제 ON/OFF |
| 세션 | `SESSION_TIMEOUT_MINUTES`, `SESSION_TABLE`, `MAX_CONCURRENT_USERS`, `CONV_LOCKS_MAX`, `CHAT_EPHEMERAL_USER_ID_PREFIXES`, `CHAT_USER_ID_IS_EPHEMERAL_IF_UUIDV4`, `DEFAULT_CONVERSATION_TITLE` | 동시 접속·비로그인 식별 규칙 |
| 되묻기/추천 | `MAX_CLARIFY_ATTEMPTS`, `CLARIFY_FAILURE_MESSAGE`, `MAX_SUGGESTED_QUESTIONS` | 시군 되묻기 최대 회수·실패 안내·추천 질문 수 |
| 파일/업로드 | `UPLOAD_DIR`, `OKMS_DOC_TABLE`, `OKMS_VIEW_TABLE`, `ALLOWED_BASE_DIRS`, `BASE_PATH_ALIASES` | 업로드 경로·OKMS DB 테이블·허용 경로 |
| 원격 텍스트 전송 | `UPLOADED_TEXT_REMOTE_*` (ENABLED/HOST/PORT/USER/DIR/TIMEOUT/SSH_KEY_PATH/PASSWORD) | 업로드 텍스트 SFTP 전송 |
| CORS | `CORS_ORIGINS` | `*` 또는 콤마 구분 도메인 목록 |
| 인사말·UI | `GREETING_MESSAGE`, `ASSISTANT_ACTION_FEEDBACK`, `ASSISTANT_ACTION_FEEDBACK_OPTIONS` | 첫 인사말, 피드백 버튼 옵션 |

`ENVIRONMENT` 값에 따라 `DEBUG`/`LOG_LEVEL`/`RAG_ENABLED`/`LLM_ENABLED` 기본값이 자동 보정됨 (development → DEBUG 활성, testing → RAG/LLM 비활성). `.env`에서 명시한 값은 덮어쓰지 않음.

---

## 7. 도메인별 구조

### 7-1. `chat` — 핵심 도메인

사용자 질문 수신 → AI 답변 생성 전체 파이프라인. 8장 참조.

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/v1/chat/completions` | 메인 채팅 응답 (스트리밍/비스트리밍) |
| POST | `/v1/chat/recommended-question` | 추천 후속 질문 응답 (의도 분석 생략, guide_recommend 전용 RAG) |
| POST | `/v1/chat/recommend/collect` | 복지 추천 시작 (지역·출생연도 수집 질문 반환) |
| POST | `/v1/chat/suggest-questions` | 후속 질문 추천 생성 (conv_id 기반 권장) |

### 7-2. `conversation` — 대화 이력

사용자별 대화 목록, 메시지 히스토리, 피드백(좋아요/싫어요) 관리.

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/v1/chat/conversations` | 사용자 대화 목록 조회 |
| POST | `/v1/chat/conversations` | 새 대화 생성 (인사말 자동 저장, 비로그인 시 nologin{conv_id}로 저장) |
| GET | `/v1/chat/conversations/{conv_id}/messages` | 대화 메시지 목록 조회 |
| DELETE | `/v1/chat/conversations/{conv_id}` | 대화 삭제 |
| POST | `/v1/chat/messages/feedback` | like/dislike/copy 피드백 저장 (action: copy/like/dislike/clear) |
| GET | `/v1/chat/history` | **deprecated** — `/conversations/{conv_id}/messages` 사용 권장 |

### 7-3. `document` — 문서 관리

사용자 업로드 파일 기반 QA (PDF, Word 텍스트 추출·요약·질의응답).

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/v1/files/upload` | 파일 업로드 |
| GET | `/v1/documents/download` | 문서 다운로드 |
| POST | `/v1/documents/extract-text` | 텍스트 추출 |
| POST | `/v1/documents/extract-and-summarize` | 텍스트 추출 + LLM 요약 |
| POST | `/v1/documents/ask` | 업로드 문서 기반 QA |

### 7-4. `rag` — RAG 보조 API

DeepServer 프록시 — triple 추출, query 확장, re-query, 비교 속성/트리플 추출.

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/v1/query/extract-triples` | 트리플(Subject-Predicate-Object) 추출 (PROMPT_ID=18) |
| POST | `/v1/query/expand` | 동의어/유사 표현 확장 (PROMPT_ID=2) |
| POST | `/v1/query/re-query` | 멀티턴 컨텍스트 기반 질의 재구성 (PROMPT_ID=17) |
| POST | `/v1/query/comparison-attributes` | 비교 의도 속성 추출 (PROMPT_ID=19) |
| POST | `/v1/query/comparison-triples` | 비교 의도 트리플 추출 (PROMPT_ID=20) |

### 7-5. `keyword` — 키워드 추출

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/v1/keywords/extract` | kiwi 기반 명사(NNG/NNP/SL) 추출 |

### 7-6. `tts` — TTS / STT

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/tts/synthesize` | 텍스트 → 음성 (단일 응답, raw_base64 반환) |
| POST | `/api/tts/stream` | 텍스트 → 음성 스트리밍 (NDJSON) |
| WS | `/api/stt/stream` | 음성 → 텍스트 WebSocket 스트리밍 (바이너리 송신 / "EOS" 텍스트로 종료) |

### 7-7. `system` — 헬스체크

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/health` | 서비스 상태 확인 |

---

## 8. 핵심: 채팅 파이프라인 흐름

`POST /v1/chat/completions` 요청 처리 흐름. **non-streaming**과 **streaming** 두 경로가 있으며, 동일한 단계 함수(`_pipeline_steps.py`)를 공유합니다.

### 8-1. 진입 및 기본 검증 (`router.py`)

```
요청 수신
 ├─ JSON 파싱 + ChatRequest 생성
 ├─ _ensure_runtime_user_id()       # 비로그인은 user_id = conv_id로 고정
 ├─ 로그 컨텍스트 설정 (conv_id, user_id)
 ├─ _merge_and_init_conversation()  # 세션 초기화 + 이전 이력 로드
 ├─ 메시지 유효성 검증
 ├─ is_clarification_answer()       # 직전 챗봇 메시지가 되묻기였는지 판정
 │
 ├─ ── More Results 라우터 분기 (non-streaming / guide_recommend / recommended-question) ──
 │   classify_next_intent()         # MORE_INFO·MORE_DETAIL·NEW_SEARCH·REFINE_SEARCH·CLARIFY_REPLY
 │   - more_detected → 직전 reformed_query 재사용 + 노출 문서·서비스명 제외 리스트 구성
 │   - more_topic_switch → 직전 주제 오염 방지(쿼리 재구성 스킵)
 │
 ├─ guide_recommend 모드면 → _handle_rag_mode(intent="guide_recommend") 바로 분기
 └─ 스트리밍이면 → _streaming_chat_flow(); 아니면 아래 단계 직접 실행
```

### 8-2. 파이프라인 단계 (`_pipeline_steps.py`)

| 단계 | 함수 | 설명 |
|---|---|---|
| **[1] Pre-Check** | `run_pre_check()` | RAG 필요 여부 + 되묻기(clarification) 감지. `is_clarification`이면 스킵 |
| **[2] 조기 시군 확인** | `run_early_sigun_check()` | 되묻기 답변(`is_clarification`)일 때 query_recreation 전에 시군 조기 확정 |
| **[3] 쿼리 재구성** | `_run_query_recreation()` | 멀티턴 문맥 반영. MORE_INFO/general 후속/NEW_SEARCH 시 스킵 |
| **[4] 범위 체크** | `run_out_of_scope_check()` | 경남 외 지역 질문 감지 → 안내 메시지 반환 |
| **[5] 시군 체크** | `run_sigun_check()` | 시·군 추출 실패 시 되묻기 (최대 `MAX_SIGUN_ASK_ATTEMPTS`회 후 포기) |
| **[6] 통합 전처리** | `run_unified_preprocess()` | LLM 1회 호출로 정제·의도·재구성·확장 동시 처리 (아래 참고) |
| **[6-MORE]** | 재사용 경로 | MORE_INFO/MORE_DETAIL이거나 general 후속이면 LLM 호출 없이 히스토리 preprocess 재사용 |
| **[7] 생애주기 체크** | `run_lifecycle_check()` | guide_recommend 전용 로그 기록 |
| **[8] RAG 실행** | `_handle_rag_mode()` | 의도별 파이프라인 선택·실행 (`process_rag_general` 등) |

### 8-3. 통합 전처리 (`preprocessing.py`)

LLM 1회 호출로 여러 결과를 동시에 반환 (API 호출 횟수·응답 시간 절감).

```
입력: 현재 질문 + 최근 3턴 대화 이력 ([이전 대화] / [현재 질문] 형식)
      ↓ LLM 1회 호출 (temperature=0, JSON 응답)
출력 (LLM):
  ├─ query              : 맞춤법·표준어 정제 질문
  ├─ intent             : general | comparison | guide_recommend | search
  ├─ intent_reason      : 의도 판단 근거
  ├─ reformed_query     : 검색 엔진 최적화 질의
  ├─ expanded_queries   : 의미 확장 질의 목록 (검색 재현율 향상)
  ├─ search_target      : (intent=search 전용) admin_local_office | welfare_facility | ambiguous
  ├─ policy_priority_tag: implant | low_income | elderly_benefits | null
  └─ detail_requested   : 상세 요청 여부 (more_detail와 합산)
출력 (kiwi 형태소 분석 — LLM 호출 후 추가):
  └─ keywords           : reformed_query에서 추출한 명사 목록 (Mariner 키워드 검색에 사용)
```

장애 관련 어휘는 `sanitize_disability_*`로 사전 정제. 의도 라벨은 `intent_registry`가 단일 진실 공급원.

### 8-4. MORE_INFO / MORE_DETAIL 처리

직전 답변에 대한 후속 발화는 `classify_next_intent()` 결과로 분기:

- **MORE_INFO**: 직전 `reformed_query`·`expanded_queries`·`intent`를 재사용하고, 직전 노출 `chunk_id` / `service_name`을 검색 제외 목록에 추가. 통합 전처리 LLM 호출 스킵. **직전 intent와 무관하게 guide_recommend로 강제**.
- **MORE_DETAIL**: `next_intent.re_query`(특정 서비스명)를 새 질의로 사용. 제외 목록은 적용하지 않음 — 동일 문서의 상세를 요구하므로.
- **NEW_SEARCH / REFINE_SEARCH**: 주제 전환 — 쿼리 재구성 스킵하고 통합 전처리부터 정상 진행.

---

## 9. RAG 파이프라인 상세 (general)

`process_rag_general()` — `src/app/chat/infra/rag/pipeline_general.py`

> **변경점 (구버전 대비)**
> - OKMS 적합성 LLM 판단 → 제거. **OKMS / GOV_OKMS / GSND를 항상 동시 병렬 수집**.
> - WELFARE_TEL 단계 → general 파이프라인에서 제거.
> - 관련성 필터(`filter_irrelevant_docs`)는 그대로 유지하되 적용 단계를 통합.
> - `more_detail` 플래그 시 후보 풀과 최종 노출 건수 확대.
>
> **단계 번호 안내**: 코드 주석의 Step 번호를 그대로 따랐습니다. Step 2는 코드에서 빠져 있고, Step 5-A·6-F·7-C·7-C-2/3/4·7-D 같은 비순차 라벨도 코드 그대로입니다. 8장 표의 [1]~[8]은 별도 레이어(`_pipeline_steps.py`)이며 본 절의 Step과 다릅니다.

```
[Step 1] 쿼리 확장
         ├─ precomputed_expanded_queries → 통합 전처리 결과 + dedupe_cap
         └─ 없으면 → expand_query() 폴백

[Step 3] 키워드(트리플) 추출
         ├─ precomputed_keywords → filter_okms_keywords() 적용
         │   (첫 확장쿼리에만 매핑, 나머지 빈 리스트)
         └─ 없으면 → extract_triples() (확장쿼리별 병렬)
         공통: _build_search_queries() → 키워드 검색 문자열 구성

[Step 4] SIGUN/LIFECYCLE/YEAR 필터 추출
         ├─ SIGUN     : 외부 인자 우선, 없으면 메시지에서 추출 → normalize_sigun()
         ├─ LIFECYCLE : 출생연도 → _birth_year_to_lifecycle() (없으면 메시지·히스토리에서 추출)
         └─ YEAR      : extract_year_filters()

[Step 5-A] OKMS (Group A) + GOV_OKMS + GSND_DATASET_V8 동시 병렬 검색
           ├─ asyncio.gather(okms_task, gsnd_task)
           ├─ Mariner 호출은 동기 → run_in_executor()로 스레드 풀
           ├─ OKMS Group A    : 벡터/키워드 듀얼 (SIGUN·YEAR·LIFECYCLE 필터, excluded_chunk_ids)
           ├─ GOV_OKMS        : LIFECYCLE·SIGUN 필터만, YEAR 없음
           └─ GSND            : 키워드 검색쿼리 + 확장쿼리, 시군 필터 미적용
                                (-60004 타임아웃 시 연도필터 해제 후 1회 재시도, 누적 시 조기 중단)

[Step 6] OKMS+GOV_OKMS 중복 제거 후 상위 N건 선택
         (more_detail=False면 8건, True면 15건)

[Step 6-F] OKMS Fallback (결과가 임계치 미만일 때)
           └─ 생애주기·사업명 앵커 제거 후 재검색 (시군·연도·제외 유지)

[Step 7] GSND 검색 결과 정리 — top 3건(more_detail=True면 7건) 보강

[Step 7 합산] OKMS + GSND 통합 후 가중치 정렬

[Step 7-C] 정책 우선순위 부스팅 + LLM 관련성 필터
           ├─ apply_policy_priority_to_documents() — 매칭 문서 가중치 부스팅
           │   (제외 목록이 있는 후속 검색에서는 비활성)
           └─ filter_irrelevant_docs() [8b/sllm] — 질문 무관 문서 제거

[Step 7-C-3] 관련성 필터 결과 0건 → Group A 하위 문서로 재시도

[Step 7-C-4] 0건이거나 제외 문서 있으면 → OKMS Fallback 재검색 + 관련성 필터 재적용

[Step 7-C-2] 최종 0건 → "답변을 찾을 수 없습니다" 반환

[Step 7-D] sort_weight_top30_then_year() — 상위 30%는 YEAR 최신순, 나머지는 WEIGHT 내림차순

[Step 8] build_referenced_documents() — 프론트 표시용 출처 메타데이터

[Step 9] 최종 응답 생성 (대형 LLM — 32b/luxia)
         └─ generate_final_response_v2()
            ├─ 의도별 프롬프트 선택
            ├─ reformed_query + 검색 문서 → 단일 user 메시지로 LLM 호출
            ├─ more_info_mode / detail_requested 시 프롬프트 분기
            └─ stream=True면 SSE 스트리밍 제너레이터 반환
```

다른 의도 파이프라인:

- **comparison**: `pipeline_comparison.py` — 비교 대상 사업명 앵커 복수화, DeepServer triple/attribute 추출
- **guide_recommend**: `pipeline_guide_recommend.py` — 생애주기 + 시군 기반 맞춤 추천
- **search**: `pipeline_search.py` — `search_target`에 따라 WELFARE_TEL / WELFARE / OKMS 분기
- **recommended-question**: `pipeline_recommended_question.py` — referenced_documents 기준 Mariner 재검색

---

## 10. Mariner 검색 엔진

`src/app/mariner/`

- **JVM 싱글톤** (`jvm_manager.py`): 프로세스당 1회 init, JPype로 Java 클래스 직접 호출
- **동기 → 비동기**: Mariner 호출은 blocking → `asyncio.run_in_executor()`로 스레드 풀 실행
- **Fallback**: 동일 쿼리셋 함수에서 `apply_business_anchor=False` / `lifecycle_filter=None` 옵션으로 호출 (별도 파일 없음)
- **응답 파싱**: `parsing.py`에서 WEIGHT·CHUNK_ID·메타 추출

| 파일 | 컬렉션 | 설명 |
|---|---|---|
| `queryset_okms.py` | OKMS | 경남 복지 서비스 카드 — 벡터+키워드 듀얼, fallback 옵션 내장 |
| `queryset_gov_okms.py` | GOV_OKMS | 정부 기관 복지 서비스 카드 |
| `queryset_gsnd.py` | GSND_DATASET_V8 | 경남도청 일반 행정 문서 (광역 정책 포함, 시군 메타 비균일) |
| `queryset_welfare.py` | WELFARE | 복지시설 정보 |
| `queryset_welfare_tel.py` | WELFARE_TEL | 읍·면·동 복지 연락처 |
| `queryset_recommended_question.py` | OKMS | 추천 후속 전용 (referenced_documents 기반 재검색) |
| `queryset_upload.py` | UPLOAD | 사용자 업로드 문서 |

---

## 11. 부가 기능

### 11-1. 정책 우선순위 워커 (`infra/rag/policy_priority.py`)

- 시작 시 `preload_policy_priority_cache()`로 DB → 메모리 로드
- 백그라운드 워커가 주기적으로 변경 감지 및 캐시 갱신
- `apply_policy_priority_to_documents()`로 매칭 문서 가중치 부스팅
- 종료 시 워커 정상 종료

### 11-2. 프롬프트 관리 (`shared/utils/prompt_loader.py`)

- `lru_cache` 데코레이터로 파일 IO 1회만
- startup 시 `_preload_prompts()`로 전체 워밍업
- 주요 프롬프트: `unified_preprocessing_prompt`, `query_recreation_prompt`, `classification_*_prompt`, `final_response_prompt`, `next_intent_prompt` 등 `prompts/` 디렉토리에 위치

### 11-3. 비로그인 사용자 / 동시 접속 제한

- `chat_user_id_requires_nologin_canonical()` — 비로그인 user_id를 conv_id로 고정해 로그인과 동일한 히스토리·전처리 경로 사용
- DB 저장 시 `nologin{conv_id}` 네임스페이스
- 동시 사용자 제한: `user_session.py`에서 `MAX_CONCURRENT_USERS`(기본 50) / `SESSION_TIMEOUT_MINUTES`(기본 10분) 기준 초과 시 차단

### 11-4. 로깅

- PID별 RotatingFileHandler (Windows 다중 프로세스 rollover 충돌 회피)
- `RequestContextFilter`로 conv_id·user_id를 모든 로그 라인에 자동 첨부
- `httpx`/`httpcore` 라이브러리 로그는 WARNING으로 억제

### 11-5. SSE 스트리밍

- `_streaming_chat_flow()`가 단계 함수를 호출하며 중간 상태 메시지를 SSE event로 전송
- `status_callback("최적의 답변방식을 찾고 있습니다")` 등 단계별 안내 메시지
- 최종 응답은 LLM 토큰 단위로 스트리밍 (delta 방식)
