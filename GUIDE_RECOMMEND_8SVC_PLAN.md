# guide_recommend 8건 보장 국소 수정 Plan (관련성 필터 **유지**)

> 전제: guide_recommend_v2가 목표 8건(OKMS 쿼터 5 + GOV_OKMS 쿼터 3) 대신 3-6건만 추천하는 문제. 원인은 RRF/리랭킹이 아니라 **SLM 관련성 필터 + 닫힌 루프 구조**로 확정(A 검증 완료). 이 문서는 설계만; 코드는 진행 신호 후 착수.

## 1. 검증된 문제 (A 테스트 결과)

동일 질의("창원 기초생활수급자 혜택"):

| | 필터 ON (현재) | 필터 OFF (A 검증) |
|---|---|---|
| LLM 입력 top_docs | **6** | **11** |

흐름: `8(OKMS5+GOV3) → D-1 필터(8→6) → D-1.5 재귀보강(→8) → D-1.6 재귀후 재필터(→6)`.
`pipeline_guide_recommend.py:661`(D-1.6)이 재귀 보강을 무위로 돌리는 **닫힌 루프** = "필터가 떨군다 → 재귀가 채운다 → 필터가 또 떨군다".

## 2. 설계 원칙 (사용자 요구 반영)

- **관련성 필터는 유지한다.** 끄면 노이즈 서비스가 추천에 섞임 → 품질 저하. (전역 토글 OFF 는 진단용일 뿐 영구 해법 아님)
- **search·general 등 다른 파이프라인은 건드리지 않는다** (국소 수정).
- **GOV_OKMS 쿼터 구조 보존** (OKMS 5 + GOV 3 독립).
- 컬렉션에 relevant 문서가 8개 미만이면 **정상적으로 8 미만**을 반환한다(이건 버그가 아니라 옳은 동작). 목표는 "relevant 가 8개 있는데도 못 채우는" 경우 제거.

## 3. 근본 원인 — 구조적 순서 오류

현재 순서: **자르고(cap) → 필터** (`pipeline_guide_recommend.py`)
- 310: `gr_top_docs = gr_group_a_top[:_GR_FINAL_TOP_N]`  ← OKMS 후보를 **5건으로 먼저 자름**
- 372: `+ gov_okms_top_docs`(3) → 8건
- 391: `filter_irrelevant_docs(...)` ← 5+3=8 에만 필터 적용 → 떨구면 5 미만
- 426~/465~: reserve·재귀로 refill (필터 없이 Mariner WEIGHT)
- 661: D-1.6 재귀분 재필터 → 다시 떨굼

문제: **필터에 들어가는 OKMS 후보가 5건뿐**이라, 필터가 2건 떨구면 곧장 미달. 이후 refill/re-filter 가 루프를 만든다.

## 4. 방안 비교

| 방안 | 내용 | 필터 유지 | 평가 |
|---|---|---|---|
| **B1 (권장)** | **필터 먼저 → 자르기.** 더 큰 후보풀(OKMS top 10 등)에 필터를 한 번 돌리고, 생존분에서 쿼터(5)만큼 취함. GOV 도 동일. | ✅ 완전 | 구조적 순서 오류를 직접 제거. self-defeat 루프 소멸 |
| B2 | 재귀 루프 안에서 매 iter 필터링하며 8 생존분까지 반복 | ✅ | 필터 호출 다수 → 느림. 루프는 유지 |
| B3(기각) | D-1.6 skip / 필터 우회 | ❌ | 사용자 우려대로 품질 저하 — **채택 안 함** |

## 5. 권장안 B1 상세

**핵심 변경: "cap → filter" 를 "filter → cap" 으로 뒤집는다.**

- **OKMS 풀**: `gr_group_a_top`(이미 top 10 확보됨, 272행)을 **자르지 않고** 필터에 투입 → 생존분에서 상위 `_GR_FINAL_TOP_N`(5)만 취함.
- **GOV 풀**: `gov_okms_top_docs` 후보(수집 5건 등)를 필터에 투입 → 생존분에서 상위 `_GR_GOV_OKMS_TOP_N`(3)만 취함. (GOV 쿼터 독립 유지)
- 두 생존 쿼터를 concat → 최대 8건, **전부 필터 검증됨**.
- 생존이 쿼터 미달일 때만 기존 reserve(D-1.4)/재귀(D-1.5) **fallback 유지** — 단 이때는 후보가 부족한 진짜 케이스이므로 D-1.6 재필터도 그대로 둔다(루프 위험 없음: 이미 큰 풀을 필터했으므로 재귀는 드물게만 발생).

### 변경 지점 (예상)
- `pipeline_guide_recommend.py:310` — OKMS `[:5]` cap 을 필터 **이후**로 이동.
- `pipeline_guide_recommend.py:382~391` — 필터 입력을 `gr_group_a_top`(+GOV 후보)로 확대, 필터 후 쿼터 cap 적용.
- `_GR_FINAL_TOP_N` 의미를 "필터 전 cap" → "필터 후 쿼터"로 정리.
- GOV 필터 적용 위치 신설(현재 GOV 는 필터 전 quota 확정 293~297행).

### 왜 이게 8을 채우나
필터가 2건 떨궈도 OKMS 후보가 10건이므로 생존이 5건 이상 남을 확률이 높다. "5건으로 잘라놓고 2건 떨구는" 현재 구조의 미달을 구조적으로 제거.

## 6. 엣지 케이스 / 주의

- **relevant < 8**: 큰 풀을 필터해도 생존이 8 미만이면 그대로 반환(올바름). 재귀 fallback 은 보조로만.
- **속도**: 필터 입력이 5→10+ 로 늘어 SLM 호출 부하 증가. 단 D-1.6 재필터·다수 재귀 iter 가 사라져 **순증 아닐 가능성**. 측정 필요.
- **GOV 쿼터**: GOV 를 필터에 넣을지(품질) vs 그대로 둘지(보장) 결정 필요. 기본은 "GOV 도 필터하되 쿼터 3 보장".
- **rejected 추적**: 재귀 fallback 시 기존 rejected_chunk_ids 제외 로직 유지.

## 7. 테스트 계획 (메모리: 수정 후 직접 실행·검증)

- A 검증에서 실패했던 질의셋(예: "창원 기초생활수급자")으로 **필터 ON 상태에서 8건 도달** 확인.
- 필터가 여전히 동작하는지(명백한 무관 문서가 추천에서 제외되는지) 확인 — 품질 회귀 없음.
- 다른 의도(search/general/comparison)는 무영향 확인.
- 서버 로그 카운트 추적: `최종 문서 / 관련성 필터 후 / 최종 top_docs`.

## 8. 롤백
변경은 guide_recommend 단일 함수 내 순서 조정 — 되돌리기 쉬움. 전역 `RELEVANCE_FILTER_ENABLED` 는 건드리지 않으므로 다른 파이프라인 영향 없음.

---
*작성일: 2026-05-24 · 상태: 검토 대기(구현 미착수) · 관련: project_guide_recommend_8svc 메모리*
